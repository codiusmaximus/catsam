"""
Code that looks up the redshift and age for a given snapshot
"""

# read in the snapshot - z list

def zreturn(snap_in)

    z_now = data_z[snap_in]
    age_now = data_age[snap_in]
    return z_now

def agereturn(snap_in)
    age_now = data_age[snap_in]
    return age_now
